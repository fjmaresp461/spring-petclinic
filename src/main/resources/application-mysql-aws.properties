# database init, supports mysql too
database=mysql
spring.datasource.url=${MYSQL_URL:jdbc:mysql://spring-petclinic-mys.cr8yaqwcc1dy.us-east-1.rds.amazonaws.com/petclinic}
spring.datasource.username=${MYSQL_USER:fjmaresp}
spring.datasource.password=${MYSQL_PASS:Admin.123}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always